list comprList = {4}

from 20 nodes in comprList do {
    filter ("TRA.destinationPort" == 1000 and "TRA.sourcePort" == 1025 )
        var sum  
        var avg = 5
        var b = 1
        
        sum = avg + b
        
}
